<?xml version="1.0" encoding="utf-8"?>
<androidx.constraintlayout.widget.ConstraintLayout xmlns:android="http://schemas.android.com/apk/res/android"
    android:layout_width="match_parent"
    android:layout_height="wrap_content"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    android:padding="4dp"
    android:background="#66999999">

    <de.hdodenhof.circleimageview.CircleImageView
        android:id="@+id/rv_search_image"
        android:layout_width="60dp"
        android:layout_height="60dp"
        app:layout_constraintStart_toStartOf="parent"
        app:layout_constraintTop_toTopOf="parent"
        app:layout_constraintBottom_toBottomOf="parent"
        android:src="@drawable/ic_launcher_background"/>

    <TextView
        android:id="@+id/rv_search_username"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        app:layout_constraintTop_toTopOf="parent"
        app:layout_constraintStart_toEndOf="@+id/rv_search_image"
        app:layout_constraintBottom_toTopOf="@+id/rv_search_name"
        android:text="username"
        android:textSize="14sp"
        android:layout_marginStart="15dp"
        android:textColor="@color/black"/>

    <TextView
        android:id="@+id/rv_search_name"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        app:layout_constraintStart_toEndOf="@+id/rv_search_image"
        app:layout_constraintTop_toBottomOf="@id/rv_search_username"
        app:layout_constraintBottom_toBottomOf="parent"
        android:text="name"
        android:layout_marginStart="15dp"
        android:textSize="14sp"
        android:textColor="#808080"/>
</androidx.constraintlayout.widget.ConstraintLayout>